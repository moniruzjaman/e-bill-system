# =============================================
# E-Bill System Environment Configuration
# =============================================

# ---------------------------------------------
# LOCAL DEVELOPMENT (SQLite)
# ---------------------------------------------
# For local development with SQLite:
# DATABASE_URL="file:./dev.db"

# ---------------------------------------------
# PRODUCTION (PostgreSQL for Vercel)
# ---------------------------------------------
# For Vercel deployment with PostgreSQL:

# Connection pooling URL (used for queries)
DATABASE_URL="postgresql://username:password@host:5432/e_bill_system?pgbouncer=true&connect_timeout=15"

# Direct database URL (used for migrations/push)
DIRECT_DATABASE_URL="postgresql://username:password@host:5432/e_bill_system"

# ---------------------------------------------
# DATABASE PROVIDERS
# ---------------------------------------------
# Vercel Postgres: Copy from Vercel Dashboard > Storage > .env tab
# Supabase: Project Settings > Database > Connection string
# Neon: Dashboard > Connection Details
# Railway: Project > Variables tab
